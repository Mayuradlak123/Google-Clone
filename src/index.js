import React from 'react';
import ReactDOM from 'react-dom/client';

import App from './App';
// import reducer, { initialState } from './Components/Redux-Components/Reducer';
// import { StateProvider } from './Components/Redux-Components/stateProvider';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render( <
    >

    <
    React.StrictMode >
    <
    App / >
    <
    /React.StrictMode> <
    />
    // <React.StrictMode>
    //     <StateProvider intialState={initialState} reducer={reducer} >
    // <App />
    // </StateProvider>
    // </React.StrictMode>

);